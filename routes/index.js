const router = require('express').Router();

router.get('/', function(req, res) {
  res.send('helllooo');
});

//require your model and post some data

//require your models to get the data

module.exports = router;
